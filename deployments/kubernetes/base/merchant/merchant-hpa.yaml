apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: merchant-hpa
  namespace: point-of-sale
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: merchant
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70

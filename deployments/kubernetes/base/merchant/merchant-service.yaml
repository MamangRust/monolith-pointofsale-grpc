apiVersion: v1
kind: Service
metadata:
  name: merchant
  namespace: point-of-sale
spec:
  selector:
    app: merchant
  ports:
    - name: grpc
      port: 50054
      targetPort: 50054
  type: ClusterIP

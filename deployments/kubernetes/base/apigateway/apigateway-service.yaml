apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: point-of-sale
spec:
  selector:
    app: apigateway
  ports:
  - name: http
    port: 5000
    targetPort: 5000
  - name: metrics
    port: 8100
    targetPort: 8100
  type: ClusterIP

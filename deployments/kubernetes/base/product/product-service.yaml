apiVersion: v1
kind: Service
metadata:
  name: product
  namespace: point-of-sale
spec:
  selector:
    app: product
  ports:
    - name: grpc
      port: 50056
      targetPort: 50056
  type: ClusterIP
